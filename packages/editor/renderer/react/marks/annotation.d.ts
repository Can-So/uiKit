import * as React from 'react';
export interface Props {
    id: string;
    annotationType: string;
    children: React.ReactNode;
}
declare const _default: ({ id, annotationType, children }: Props) => JSX.Element;
export default _default;
