import { Node as PMNode, Schema } from 'prosemirror-model';
export declare function normalizePMNodes(nodes: PMNode[], schema: Schema): PMNode[];
export declare function isNextLineEmpty(input: string): boolean;
